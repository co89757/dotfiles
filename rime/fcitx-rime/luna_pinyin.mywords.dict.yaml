---
name: luna_pinyin.mywords    # 词库名称，与文件名一致
version: "1.0"                # 版本号，任填
sort: by_weight               # 排序依据，by_weight表示按权重（词频）排序
use_preset_vocabulary: true   # 是否使用预置发音，一般选true
# 此处为扩充词库默认一并载入的词库
import_tables:
  - luna_pinyin               # 预置的朙月拼音词库
  - luna_pinyin.website       # 網站
---